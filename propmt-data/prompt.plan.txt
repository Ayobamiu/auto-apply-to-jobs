when scraping job from handshake, we basically do these steps:
  - runJobScraper [/backend/orchestration/run-pipeline.ts#L102]
    - getJobFromUrl [/backend/agents/job_scraper_agent/index.ts#L52]
      - check if cached
      - doScrape [/backend/shared/job-from-url.ts#L292]
        - get auth session from db
        - open url in browser
        - get Job description
        - scrapeJobFromPage [/backend/shared/job-from-url.ts#L120]
          - check if already submitted [important to stop further scraping]
          - check apply button type [important to stop further scraping e.g. if apply_type is apply_externally]
          - check if job is closed [important to stop further scraping]
          - return ScrapedJob [/backend/shared/types.ts#L360]
    - return job
    - check if isHandshakeAuthWallScrape [/backend/shared/scrape-auth-wall.ts#L20]
    - return job
  - probeRequiredSections [/backend/orchestration/run-pipeline.ts#L153]
    - check cache for presentSections and existingForms
    - get auth session from db
    - open url in browser
    - click apply button to open modal
    - get presentSections 
    - save in application_forms
    - processDynamicForm [/backend/shared/probe-apply-modal.ts#L114]
      - check existing
        - 
      - extractHandshakeForm [backend/shared/form-extraction/handshake-extractor.ts#L297]
    - generate answer
